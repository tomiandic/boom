module.exports = {
  siteMetadata: {
    siteUrl: `https://www.boomevents.hr`,
  },
  plugins: [
    `gatsby-plugin-image`,
    `gatsby-plugin-sharp`,
    `gatsby-transformer-sharp`, // Needed for dynamic images
  ],
};
